# 🤝 贡献指南

感谢你有兴趣为Frontend Test Agent贡献代码！我们欢迎任何形式的贡献，包括但不限于：

- 🐛 **Bug报告**
- 💡 **功能请求**
- 📝 **文档改进**
- 🔧 **代码贡献**
- 🎨 **UI/UX优化**
- 📖 **教程和示例**

## 📦 快速开始

### 开发环境搭建

```bash
# 1. 克隆仓库
git clone https://github.com/yourusername/frontend-test-agent.git
cd frontend-test-agent

# 2. 安装依赖
npm install

# 3. 构建项目
npm run build

# 4. 运行测试
npm test

# 5. 开发模式（实时编译）
npm run dev
```

## 🚀 贡献流程

### 1. 寻找任务

- 查看 [GitHub Issues](https://github.com/yourusername/frontend-test-agent/issues)，寻找标记为 "good first issue" 或 "help wanted" 的任务
- 如果你有新的想法或功能建议，可以先创建一个Issue进行讨论

### 2. Fork仓库

点击页面右上角的 "Fork" 按钮，将仓库Fork到你自己的GitHub账号下

### 3. 创建分支

```bash
# 创建并切换到新分支
git checkout -b feature/your-feature-name
# 或
git checkout -b bugfix/your-bug-fix
```

### 4. 开发

- 编写代码
- 确保代码符合项目的编码规范
- 编写相关的单元测试
- 运行测试确保所有测试通过

### 5. 提交代码

```bash
# 添加修改的文件
git add .

# 提交更改（请使用英文提交信息）
git commit -m "feat: add new feature XYZ"
# 或
git commit -m "fix: resolve issue with ABC"
```

**提交信息规范**：

- 使用英文提交信息
- 开头使用类型前缀：
  - `feat:` 新功能
  - `fix:` Bug修复
  - `docs:` 文档更新
  - `style:` 代码格式调整
  - `refactor:` 代码重构
  - `test:` 测试相关
  - `chore:` 构建流程或辅助工具的变动

### 6. Push到远程仓库

```bash
git push origin feature/your-feature-name
```

### 7. 创建Pull Request

1. 访问你Fork的仓库页面
2. 点击 "Compare & pull request" 按钮
3. 填写详细的PR描述，包括：
   - 变更的内容
   - 解决的问题（如果有）
   - 测试情况
   - 截图（如果涉及UI变更）
4. 提交Pull Request

### 8. 代码审查

维护者会对你的PR进行审查，可能会提出一些修改建议。请积极回复并进行修改。

## 📝 编码规范

### TypeScript/JavaScript

- 使用TypeScript进行开发
- 遵循 [Airbnb JavaScript Style Guide](https://github.com/airbnb/javascript) 基本规范
- 使用 `eslint` 进行代码检查
- 使用 `prettier` 进行代码格式化

### 命名规范

- 文件和目录名：使用 kebab-case
- 变量名和函数名：使用 camelCase
- 类名：使用 PascalCase
- 常量：使用 UPPER_SNAKE_CASE

### 注释规范

- 所有公共API都需要有详细的JSDoc注释
- 复杂的逻辑需要有单行注释
- 注释使用英文

示例：

```typescript
/**
 * Generate test cases for frontend components
 * @param dir Directory containing frontend components
 * @param options Generation options
 * @returns Generation results
 */
async generateTests(
  dir: string,
  options: GenerateOptions
): Promise<GenerateResult> {
  // Initialize result object
  const result: GenerateResult = {
    componentsAnalyzed: 0,
    testFilesGenerated: 0,
    coverageEstimate: 0,
    timeTaken: 0
  };
  
  // ... implementation
  
  return result;
}
```

## 🧪 测试规范

- 所有新功能都需要添加相应的单元测试
- 单元测试放在 `tests/` 目录下
- 测试文件命名：`[module].test.ts`
- 测试覆盖率目标：90%+ 代码覆盖率

### 运行测试

```bash
# 运行所有测试
npm test

# 运行特定测试
npm test -- --testPathPattern="ai/TestGenerator"

# 生成覆盖率报告
npm test -- --coverage
```

## 📖 文档贡献

### 类型

- **API文档**：使用JSDoc注释
- **用户文档**：`README.md` 和 `docs/` 目录下的文件
- **教程和示例**：`examples/` 目录下的示例代码

### 规范

- 使用Markdown格式
- 中文文档使用中文标点
- 英文文档使用英文标点
- 保持文档的一致性和准确性

## 🐛 Bug报告

### 如何提交有效的Bug报告

1. **标题**：简洁明了地描述问题
2. **版本**：使用的Frontend Test Agent版本
3. **环境**：操作系统、Node.js版本、前端框架等
4. **复现步骤**：详细描述如何复现问题
5. **预期结果**：你期望的行为
6. **实际结果**：实际发生的行为
7. **截图/日志**：如果可能，提供截图或错误日志

**示例**：

```
标题：生成Vue组件测试时出现语法错误

版本：1.0.0
环境：macOS 14.5, Node.js 20.10.0, Vue 3.4.0

复现步骤：
1. 安装frontend-test-agent
2. 创建一个Vue 3组件
3. 运行 `test-agent generate src/components --framework vue`
4. 查看生成的测试文件

预期结果：
生成可正常运行的Vue组件测试用例

实际结果：
生成的测试文件中存在语法错误，无法运行

错误日志：
SyntaxError: Unexpected token '<'
    at Object.compileFunction (node:vm:360:18)
```

## 💡 功能请求

### 如何提出有效的功能请求

1. **背景**：为什么需要这个功能？解决了什么问题？
2. **描述**：详细描述功能的具体行为
3. **示例**：如果可能，提供使用示例
4. **优先级**：这个功能的重要性如何？
5. **实现思路**：如果你有想法，可以提出实现思路

## 📜 行为准则

### 我们的承诺

为了营造一个开放和友好的环境，我们作为贡献者和维护者承诺让参与我们的项目和社区成为一个无骚扰的体验。

### 我们的标准

有助于创建积极环境的行为包括：

- 使用友好和包容的语言
- 尊重不同的观点和经验
- 优雅地接受建设性批评
- 关注社区的最佳利益
- 对其他社区成员表现出同理心

不可接受的行为包括：

- 使用性暗示的语言或图像，以及性关注或性挑逗
- 人身攻击、侮辱性/贬损性评论，以及政治攻击
- 公开或私下骚扰
- 未经明确许可，发布他人的私人信息（如物理地址或电子地址）
- 其他在职业环境中被认为不适当的行为

## 📞 联系方式

如果你有任何问题，欢迎通过以下方式联系我们：

- **GitHub Issues**: [提交问题](https://github.com/yourusername/frontend-test-agent/issues)
- **Discord 社区**: [加入讨论](https://discord.gg/xxx)
- **Twitter**: [@yourusername](https://twitter.com/yourusername)

## 🙏 致谢

感谢所有为这个项目做出贡献的开发者！你们的贡献让这个项目变得更好。

---

**记住：** 没有太小的贡献！任何帮助都是有价值的。